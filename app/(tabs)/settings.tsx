import { View, Text } from "react-native";

const Settings = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="font-geist text-xl">Settings</Text>
    </View>
  );
};

export default Settings;
